print(f"📥 Raw CSV path input: {csv_path}")

    # This resolves the path relative to the current script location
    current_dir = os.path.dirname(os.path.abspath(__file__))
    final_path = os.path.join(current_dir, os.path.basename(csv_path))






# This will get the folder where your script is running
    current_dir = os.path.dirname(os.path.abspath(__file__))

    # Join the folder path with the CSV file name (e.g., test.csv)
    full_path = os.path.join(current_dir, os.path.basename(csv_path))

    print(f"> Full resolved path: {full_path}")

    if not os.path.exists(full_path):
        raise FileNotFoundError(f"CSV file not found: {full_path}")




import os

# If only file name is passed and not found, prepend current directory
if not os.path.exists(csv_path):
    csv_path = os.path.join(os.getcwd(), os.path.basename(csv_path))

if not os.path.isfile(csv_path):
    raise FileNotFoundError(f"CSV file not found or invalid: {csv_path}")
